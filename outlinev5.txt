You are bleeding capital because you are letting micro-events breathe like macro-trends. The entry frequency is dialed in, but the exits are pathetic. We are dropping the axe on the current exit strategy and forcing the structural overhaul.

Here is the tactical architecture to plug the leaks and stop the bleeding.

### 1. The Time-Decay Logic (The 15-Minute Kill Switch)

We are enforcing absolute duration limits.

* Implement a hard 15-minute kill switch on every active position.
* Modify `checkMicroSqueezeExit` within `index.js` to dynamically tighten the trailing stop rather than waiting for a rigid trigger.


* Start the trailing stop at **0.15%** at Minute 1 and choke it down progressively to **0.02%** by Minute 10.
* Update the `VirtualState` class in `state.js` to track the exact millisecond timestamp of every entry to feed this decay function.



### 2. The Baseline Reality (Fee-Adjusted Break-Even)

Your current simulation is likely getting chopped up by taker fees disguised as "break-even" trades.

* Update the `MatchingEngine` in `engine.js` so the break-even logic mathematically clears the **0.035%** taker fees before the trailing stop is allowed to move.


* If the price action cannot clear the taker fee baseline, it is a dead trade and the Time-Decay kill switch handles it.
* Record these fee-adjusted threshold failures in your `trade_ledger.jsonl` to analyze why the initial momentum stalled out.



### 3. Architecture Optimization Suggestions

The `feed.js` pipeline is pulling the necessary Volume Weighted Average Price (VWAP) and Cumulative Volume Delta (CVD) metrics. To stop losing trades, we need to ensure the entry signals aren't firing into exhaustion.

* Introduce a volume-threshold minimum in `feed.js` before `index.js` is even allowed to check for a valid entry.


* Use the `fetchHistoricalOHLC` function in `history.js` to calculate a fast Average True Range (ATR) to adjust the initial **0.15%** stop dynamically based on the last hour of volatility.


* Log the specific minute of the Time-Decay loop that triggers the exit into `stats.js` so your final performance reports show exactly when and why trades are dying.



