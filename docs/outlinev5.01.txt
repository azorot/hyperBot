Here is the updated blueprint. Save this block directly as your documentation. The narrative is locked into absolute capital preservation and zero tolerance for stagnation.

---

### `outlinev5.01.txt`: V5.01 Architecture & Execution Protocol

**Objective:** Transition from survival scalping to yield optimization. Eradicate structural fee-bleed through maker execution, mandate volatility prerequisites, and weaponize time-decay. We do not trade on hope; we trade on mathematical thresholds.

#### 1. The Post-Trade Reality Reset (Dynamic Adaptation)

* **Trigger:** The exact tick a trade is finalized and closed in `engine.js`.
* **Action:** Bypass the outdated static 15-minute refresh window. Immediately call a force-reset function in `state.js`.
* **Execution:**
* Recalculate total available equity.
* Recalibrate the dynamic position sizing based strictly on the newly secured balance.
* Flush the `CVD 60s` and `Spike 5s` arrays in `feed.js` to ensure the next entry isn't triggered by the residual ghost volume of the previous move.



#### 2. Volatility-Gated Entries (The ATR Hurdle)

* **Target File:** `index.js`
* **Logic:** Do not deploy capital into a dead grid. We require existing market energy to clear the 0.07% round-trip exchange tax.
* **Execution:** Before `checkVWAPEntry` evaluates CVD spikes, it must cross-reference the 14-period ATR from `history.js`.
* **The Rule:** $\text{ATR} > (\text{Entry Price} \times 0.0015)$
* If the current ATR fails this threshold, the bot stands down.



#### 3. The Momentum Snap (Linear-to-Logarithmic Decay)

* **Target File:** `state.js`
* **Logic:** The baseline decay is linear, but we weaponize it against reversals. We do not give the market room to breathe if order flow shifts.
* **Execution:**
* **The Baseline:** Implement a standard linear time-decay, starting at **0.15%** and dropping to a **0.02%** floor over 10 minutes.
* **The Trigger:** Monitor the `Spike 5s` array. If sudden adverse volume prints (e.g., a massive BUY spike while holding a SHORT), the engine detects the reversal.
* **The Snap:** The millisecond the reversal is detected, `state.js` abandons the linear path. It instantly recalculates the stop using a steep logarithmic curve, snapping the trailing stop violently tight against the current price to choke out the trade and lock in capital.



#### 4. The Trailing Shadow Limit (Maker Rebate Capture)

* **Target File:** `engine.js` (Simulated) / Live API Pipeline
* **Logic:** Deprecate market selling for standard exits. Cut the round-trip toll in half by capturing the maker fee structure.
* **Execution:**
* When an exit is triggered, the engine immediately posts a Post-Only Limit Order at the current micro-spread boundary.
* As the Time-Decay tightens the stop in `state.js`, the bot actively sends API updates (`modify` order) to drag the active limit order against the current price.
* It acts as a physical wall, forcing the execution on our terms and securing the rebate.